Start the CCA-F course — runs onboarding (Session 0) then recommends Session 1.

Use `.claude/skills/start-course/SKILL.md`.
Also treat plain-language `start`, `start course`, and `start next lesson` as valid entry signals.
