Start a specific course session (0–12).

Use `.claude/skills/start-session/SKILL.md`.
Argument hint: `<session number 0-12>`.
Also treat plain-language `Let's do Session X`, `start session X`, and `Session X` as valid entry signals.
