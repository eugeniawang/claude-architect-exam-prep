# Reference Course Placeholder

Brownfield rebuilds normally copy the original course into this folder first,
then rebuild from that preserved snapshot.

The original pre-normalization course tree is not duplicated here because the
main repository already contains the active course materials.

Use this folder as the documented reconstruction location when demonstrating the
brownfield process.
